# InkSync — Root .htaccess
# Place this file as /.htaccess

# Disable directory listings (never show folder contents in browser)
Options -Indexes

# Block direct access to sensitive file types
<FilesMatch "\.(sql|env|log|txt|md|gitignore)$">
    Require all denied
</FilesMatch>
